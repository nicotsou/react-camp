const todos = [
  { text: 'Learn React', important: true, done: false },
  { text: 'Brew Coffee', important: false, done: true },
  { text: 'Make Awesome App', important: true, done: false },
  { text: 'Watch Netflix', important: false, done: false },
];

// add a new item to a collection
// text is mandatory but `important` and `done`
// are `false` by default
const add = (/* */) => { }

// remove one item from the list
const remove = (index) => { }

// find all items with given status,
// given importance and given text fragment
const find = () => {};
